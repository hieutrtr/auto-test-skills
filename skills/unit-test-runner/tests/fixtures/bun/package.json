{
  "name": "fixture-bun",
  "version": "0.0.0",
  "private": true,
  "description": "T-1.3 fixture: minimal Bun-test project. No external test framework; `bun test` is built into the bun runtime. Detector should pick framework=bun via the bun.lockb marker + the `bun test` token in the test script (no jest/vitest/mocha/playwright in devDeps).",
  "scripts": {
    "test": "bun test"
  },
  "devDependencies": {}
}
