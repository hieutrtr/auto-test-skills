// T-1.3 fixture file (not executed by the detector — detector only inspects
// package.json + config files + lockfiles. This file is a placeholder so
// the fixture looks like a realistic project on disk).
test("sample", () => {
  expect(1 + 1).toBe(2);
});
