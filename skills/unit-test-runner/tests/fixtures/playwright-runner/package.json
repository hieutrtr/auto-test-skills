{
  "name": "fixture-playwright-runner",
  "version": "0.0.0",
  "private": true,
  "description": "T-1.3 fixture: minimal @playwright/test project used as a unit-test runner (no dev-server / no browser navigation in this fixture — that case belongs to the Phase 2 browser-test skill). Has playwright.config.ts + @playwright/test in devDependencies + script `playwright test`.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
