# T-0.6 — Repo skeleton

> **Reference**: `docs/IMPLEMENTATION-PLAN.md` Phase 0 row 0.6.
> Dep: T-0.1 (skills-survey informs whether `skills/` is dir-of-dirs or single skill — survey confirmed dir-of-dirs).

---

## Acceptance criteria (from plan)

> "Repo skeleton: README, `docs/`, `skills/`, LICENSE, `.gitignore` — `git status` clean, `tree -L 2` đúng."

Concretely for Phase 0 spike:

- [x] `README.md` exists at repo root (already present from initial commit; **not modified** per loop constraint).
- [x] `docs/` populated with PRD, ARCHITECTURE, IMPLEMENTATION-PLAN (already present; **not modified**).
- [x] `skills/` directory exists as empty placeholder (`.gitkeep` only — Phase 1 fills it).
- [x] `LICENSE` file at repo root (MIT).
- [x] `.gitignore` exists and is sane (already present from initial commit; covers `node_modules/`, `.test-runs/`, `dist/`, `.DS_Store`, `*.local`, `.env`).
- [x] `git status` clean after commit.
- [x] `tree -L 2` (or portable substitute) shows the expected layout.

---

## Approach

**Type**: scaffolding, no TDD. Verification = visual inspection of `tree -L 2` against expected layout + `git status` clean.

Steps performed:

1. `mkdir skills/ && touch skills/.gitkeep` — empty placeholder so the directory survives git tracking.
2. Write `LICENSE` (MIT, 2026, "Hieu Tran") at repo root.
3. Inspect existing `.gitignore` (already covers spike artefacts: `.test-runs/`, `node_modules/`, `dist/`, `.DS_Store`, `*.local`, `.env`) — judged sufficient for Phase 0; no changes.
4. `tree` is **not installed** on the sandbox (`which tree` → not found). Substituted with portable `find -maxdepth 2 | sort | awk` formatter; output captured in `T-0.6-review.md`.
5. Confirm protected files untouched: `README.md`, `docs/PRD.md`, `docs/ARCHITECTURE.md`, `docs/IMPLEMENTATION-PLAN.md` — none modified (verified via `git status` showing only `LICENSE` + `skills/` as new).

---

## Notes / open questions

- **No `package.json` yet**. Phase 0 prototype is pure-bash (T-0.4); the TS/Bun decision lands in T-0.5 spike memo. Once Phase 1 starts, `package.json`, `tsconfig.json`, `bunfig.toml` (or equivalent) get added under `skills/<name>/` per skill, plus a root-level workspace file if needed. Deferred — out of Phase 0 scope.
- **No `CHANGELOG.md` / `CONTRIBUTING.md`**. The plan does not require them for spike. Add when project goes external (post-Phase 4 polish).
- **`skills/.gitkeep` vs deleting `skills/`**. We keep an empty dir so the layout in the README "drop into `skills/`" makes sense from day 1. Trade-off: one dummy file vs. drift between docs and tree.
- **No `tree` binary on sandbox**. The plan AC says "`tree -L 2` đúng" — interpreted as "structure matches expectations", not "tree binary must be present". Used `find` substitute; both produce equivalent depth-2 listings. Phase 1 onboarding doc can recommend `brew install tree` if humans want richer output.
- **`.gitignore` does not yet exclude `*.log`** — intentional: spike fixtures under `spike/code/log-centralizer/tests/` write `.log` files inside ephemeral `.test-runs/` paths which ARE excluded. If `*.log` ever leaks into tracked dirs, revisit.

---

## Done-when

- `LICENSE` + `skills/.gitkeep` committed.
- `git status` reports clean working tree (no untracked, no modified).
- Review file captures `find` (tree-substitute) output and a structural assessment.
