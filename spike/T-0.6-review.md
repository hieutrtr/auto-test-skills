# T-0.6 — Repo skeleton review

**Files added**

- `LICENSE` — 21 lines, MIT, copyright 2026 Hieu Tran
- `skills/.gitkeep` — 0 bytes (placeholder for Phase 1 skill folders)
- `spike/T-0.6-repo-skeleton.md` — task file

**Files NOT modified** (per loop constraint, verified via `git diff HEAD`):

- `README.md`, `docs/PRD.md`, `docs/ARCHITECTURE.md`, `docs/IMPLEMENTATION-PLAN.md` → 0 lines diff.

---

## Layout snapshot (`tree -L 2` substitute)

`tree` is not installed on the sandbox; used `ls -1A` walk for portable depth-2 output. Verified post-commit (so `skills/.gitkeep` shows up but no untracked junk).

```
.
├── .claude/                  ← local Claude Code settings (not committed in spirit, but settings.local.json is present)
│   └── settings.local.json
├── .gitignore                ← node_modules/, .test-runs/, dist/, .DS_Store, *.local, .env
├── LICENSE                   ← MIT, new in this commit
├── README.md                 ← initial bootstrap doc, untouched
├── docs/
│   ├── ARCHITECTURE.md
│   ├── IMPLEMENTATION-PLAN.md
│   └── PRD.md
├── skills/                   ← NEW — empty placeholder for Phase 1
│   └── .gitkeep
└── spike/                    ← all Phase 0 deliverables
    ├── INDEX.md
    ├── T-0.1-review.md
    ├── T-0.1-skills-survey.md
    ├── T-0.2-hello-test.md
    ├── T-0.2-review.md
    ├── T-0.3-log-centralizer-design.md
    ├── T-0.3-review.md
    ├── T-0.4-log-centralizer-proto.md
    ├── T-0.4-review.md
    ├── T-0.5-review.md
    ├── T-0.5-success-criteria.md
    ├── T-0.6-repo-skeleton.md
    ├── code/                  ← log-centralizer prototype + tests (T-0.3, T-0.4)
    └── skills-survey.md       ← T-0.1 deliverable
```

`git status` after commit: **clean** (`nothing to commit, working tree clean`).

---

## Self-review checklist

- [x] **AC measured?** Yes.
  - "README, `docs/`, `skills/`, LICENSE, `.gitignore`": ✓ all present.
  - "`git status` clean": ✓ verified post-commit.
  - "`tree -L 2` đúng": ✓ portable substitute matches expected layout — root has 7 entries (5 expected + `.claude/` local settings + `spike/` for Phase 0 work), each subdir is shallow.
- [x] **Reproducible (1 command)?** Yes:
  - `mkdir -p skills && touch skills/.gitkeep && cp <license-template> LICENSE && git add skills LICENSE && git commit -m "..."` — three lines, deterministic.
  - Layout snapshot regenerable via the `ls -1A` walk in `T-0.6-repo-skeleton.md`.
- [x] **Failure mode documented?** Yes (see "Findings" §3 below): `skills/.gitkeep` will be removed automatically as Phase 1 lands real `skills/<name>/` folders, which makes the placeholder self-cleaning. If anyone deletes `skills/` outright, `git status` stays clean (empty dir + ignored gitkeep would not be tracked anyway).

---

## Findings (for spike memo)

1. **Repo layout is minimal and correct for spike phase.** 5 of 5 plan-mandated entries present (`README`, `docs/`, `skills/`, `LICENSE`, `.gitignore`); no premature scaffolding (no `package.json`, no `tsconfig.json`, no CI yaml) — TS-vs-Bun decision is still pending T-0.5 memo, so locking config now would be wasted churn.
2. **`.gitignore` is sufficient as-is.** Existing 6 lines (`node_modules/`, `.test-runs/`, `dist/`, `.DS_Store`, `*.local`, `.env`) cover all artefacts produced by Phase 0 prototype (`spike/code/log-centralizer/tests/` writes ephemera under `.test-runs/`). No additions needed; revisit when `*.log` or `coverage/` paths emerge in Phase 1.
3. **`skills/.gitkeep` is a known-throwaway.** Phase 1.1 (`test-log-centralizer/SKILL.md`) will populate `skills/test-log-centralizer/`, at which point `.gitkeep` becomes redundant and should be removed in that PR. Document this in `INDEX.md` or Phase-1 onboarding so it isn't carried forward as dead weight.
4. **`tree` binary missing on sandbox** — minor friction, not a blocker. The plan AC says "`tree -L 2` đúng" (correct, not "must use tree"). Captured layout via `ls -1A` walk; output matches what `tree -L 2 -a -I '.git'` would produce. Phase 1 onboarding could note `brew install tree` for humans, but no script depends on it.
5. **No CI / hooks committed yet.** Intentional — Phase 0 is single-developer spike, and `spike/code/log-centralizer/tests/run-all.sh` is invoked manually. Phase 1 should add a `.github/workflows/test.yml` that wraps `run-all.sh` plus the future skill-runner test suite. Tracked as Phase 1 prerequisite.

---

## Issues / pivot

- **None.** Pure scaffolding task, zero AC misses, zero protected-file modifications. Task closes cleanly; no follow-up needed beyond the `.gitkeep` cleanup noted in finding §3.
