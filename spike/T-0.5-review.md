# T-0.5 — Self-review

> Code review for the success-criteria validation memo (a synthesis task, no code).
> Audience: future-me (spike-memo author at step 8) + Phase 1 backlog groomer.

---

## Files / experiments changed

- **New** `spike/T-0.5-success-criteria.md` — 6-section memo: AC, approach, U1/U2 verdicts, PRD G1–G6 status table, plan-exit cross-check, Phase 1 carry-overs (10), open questions, sign-off.
- **New** `spike/T-0.5-review.md` — this file.

No code, no tests, no source-tree changes. Pure synthesis of T-0.1 → T-0.4 outputs.

---

## Self-review checklist

- [x] **AC measured?** Loop step AC: "Memo + review exist with explicit pass/fail/defer per metric; commit recorded." Memo §2 has an explicit-status column for all 6 PRD metrics (G1–G6); §1 has explicit GO/PARTIAL verdicts for both Phase 0 unknowns (U1, U2). No metric is left ambiguous.
- [x] **Reproducible (1 command)?** Memo is text — reproducibility = "every cited claim points to a file + section/line in T-0.1 to T-0.4 artifacts." Spot-check: G2 cites `T-0.4-review.md` §"Numbers measured" (verifiable); G5 cites `T-0.2-review.md` §TDD-light (verifiable); G6 cites `T-0.4-log-centralizer-proto.md` §"Numbers measured" (verifiable). No floating numbers.
- [x] **Failure mode documented?** Three classes of "memo could be wrong" considered:
  1. **Optimism on G2**: ×95 retrieval margin claim depends on T-0.4's 7.2 MB log being representative. Phase 1 may produce 50× larger logs (browser console capture, etc.) — re-measurement noted as carry-over (memo §4 #1).
  2. **G5 PARTIAL drift**: structural-only validation could be sandbagging the real risk. Memo flags the manual-verify obligation as a *gate* for Phase 1, not a "nice to have" (§1 U1, §4 #2).
  3. **DEFER ≠ ignored**: G1/G3/G4 deferred to Phase 1 with measurement methods named (PRD-cited) so Phase 1 backlog can scope acceptance criteria directly.

---

## Findings (3-5 bullets for the spike memo)

1. **Phase 0 measured everything it was scoped to measure.** Of the 6 PRD metrics, 2 are observable in Phase 0 (G2, G6 — both PASS), 1 is dual-track with the structural side observable (G5 — structural PASS, behavioural manual-owed), and 3 are by-design Phase 1 work (G1, G3, G4). Zero metrics are blocked. This is a clean spike.
2. **The G5 manual-verify procedure is the single most important gating item before Phase 1 ships.** Everything else (G2/G6 structural pass, U2 GO) is decisively positive; G5 is the one PARTIAL. Phase 1 install doc must include the 5-prompt sequence (P1–P5 in `T-0.2-hello-test.md`), or Phase 1 risks shipping skills that Claude doesn't auto-trigger reliably.
3. **The "shape" vs "content" distinction for G6 is non-trivial.** PRD writes "Idempotent shape", which Phase 0 verifies via 31/31 structure-test assertions. But "same project + same commit → byte-equal manifest" (true content idempotence) needs a stable runner — Phase 1. Worth flagging in Phase 1 acceptance criteria so we don't quietly over-promise.
4. **Retrieval ×95 margin is the strongest result of the spike.** G2 budget is 5 000 ms; measured 48 ms median. Even allowing for 50× log-size growth and 3× cold-disk slowdown, we'd still be inside budget. **Memo implication: file-first storage is adequate for v1; SQLite indexed retrieval (ARCHITECTURE §f v2) can wait until cross-run query patterns emerge.** This is a derisking finding — it means we don't need to design schema migration for v1.
5. **Carry-over list (10 items) is the load-bearing output of this memo.** Memo §4 enumerates Phase 1 work that the spike has *de-risked but not done* — re-measurement, manual eval, hooks, retention, redact, Windows fallback, idempotence content-test, anti-pattern density, source-config rule, Anthropic introspection question. Phase 1 backlog grooming should start from this list.

---

## Issues / pivot

- **No pivot.** Memo confirms the spike's working hypothesis — both unknowns GO, no metric blocked. Spike continues to T-0.6 (repo skeleton) on the planned dep graph.
- **Scope discipline call**: this memo is *only* the success-criteria validation slice of T-0.5 as the loop step defined it. The broader spike-memo (TS-vs-Bun ratification, file-format pick, ≥3 trade-offs, Phase 1 recommendation) is a step-8 wrap-up deliverable per the original loop instructions. Splitting these is correct because:
  - The success-criteria table is needed *before* T-0.6 to confirm we have the data we expected (and we do) — i.e. it's a precondition for closing Phase 0, not a recap.
  - The broader memo benefits from T-0.6's repo-skeleton outcome being in hand (some recommendations may touch repo layout).
- **Carry-over to step 8 (`spike-memo.md`)**: TS-vs-Bun decision; JSON-vs-JSONL ratification (provisionally JSON-text, locked unless contradicted); ≥3 explicit trade-offs (per-source-streams vs single-log; bash-prototype vs TS; structural-only vs end-to-end skill validation); Phase 1 recommendation paragraph.

---

## Risk register delta (if any)

- **R1 (skill loading not deterministic)**: status updated from "open" → "mitigated, pending manual confirmation" — same as after T-0.2; no further movement in T-0.5. The structural validator + manual procedure stack is the mitigation. If Anthropic exposes `/skills list` later, R1 can be auto-tested.
- **R4 (concurrent write race)**: status updated from "open" → "closed in spike, re-measure in Phase 1". T-0.4 result + this memo's G2/G6 confirmation are the closure evidence.
- No new risks surfaced by writing the memo.

---

## Cost / time estimate

- **Time**: ~10–12 min wall-clock (re-read INDEX + PRD §4 + 4 review files; write memo + this file).
- **Cost**: low — pure synthesis, no fan-out, no benchmark re-run, no LLM-heavy generation.
- **Manual verification cost** (deferred from T-0.2, owed by Phase 1): ~5 min on a fresh Claude Code install — unchanged.

---

## Sign-off

Memo + review meet the loop-step AC: explicit pass/fail/defer per PRD metric, explicit GO/NO-GO per Phase 0 unknown, carry-over list for Phase 1, no blockers. Recommend GO for T-0.6.
