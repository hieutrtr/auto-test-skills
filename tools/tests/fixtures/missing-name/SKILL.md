---
description: Frontmatter present but no `name` key. Should fail linter check 4. Long enough to satisfy the description-length heuristic (>= 40 chars).
---

# missing-name

Body present so the body-content check passes; the failure should be the missing `name:` field.
